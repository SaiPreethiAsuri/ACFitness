apiVersion: v1
kind: Service
metadata:
  name: acfitness-app-canary-service
spec:
  selector:
    app: acfitness-app
    version: canary
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
