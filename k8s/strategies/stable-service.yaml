apiVersion: v1
kind: Service
metadata:
  name: acfitness-app-stable-service
spec:
  selector:
    app: acfitness-app
    version: stable
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
