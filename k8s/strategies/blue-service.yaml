apiVersion: v1
kind: Service
metadata:
  name: acfitness-app-blue-service
spec:
  selector:
    app: acfitness-app
    version: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
